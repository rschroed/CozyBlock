import { PIECE_IDS } from '../../pieces';

const firstBlock = {
  id: 'world-0-first-block',
  name: 'First Block',
  board: [
    [1, 1],
    [1, 1],
  ],
  pieceIds: [PIECE_IDS.SQUARE2],
};

export default firstBlock;
